# Task Queue

Last updated: 2026-06-14

This file was absent at the start of `TASK-SOURCE-REGISTRY-001`. It is now the local task queue
for source-platform work in this repository.

## Completed

Status note: older `implemented locally` rows are historical accepted work in this repository.
Rows marked `closed in main` have been explicitly reconciled against current `main`.

| Task | Status | Notes |
| --- | --- | --- |
| `TASK-OFFICIAL-SOURCES-BOA-METADATA-BACKFILL-CATCHUP-001` | completed on VPS | Ran bounded BOA metadata-only catch-up on the official-sources VPS for `2026-04-21..2026-06-05`, explicitly including the EduBecas sample date `2026-06-05`. Result: `official_documents_boa=1384`, `document_files(raw_api_response)=1384`, `source_candidates_boa=0`, `artifact_download_attempts_boa=0`, DB validation green before/after, and backups verified with `quick_check`. Activation remains partial because BOA document URL columns are not populated from the date endpoint. Report: `docs/reports/boa-metadata-backfill-catchup-2026-06-14.md`. |
| `TASK-SOURCE-PLATFORM-001` | accepted | Cross-project boundary accepted in `PROJECT_STATE.md` and `docs/CROSS_PROJECT_INTEGRATION_MAP.md`. |
| `TASK-SOURCE-REGISTRY-001` | implemented locally | Canonical executable registry added at `config/sources.yaml`, with validation tests and read-only CLI coverage reporting. |
| `TASK-SOURCE-RSS-MONITOR-001` | closed in main | Metadata-only RSS/Atom discovery monitor is present in `main` with BOCYL as the original pilot. Default mode is preview; JSONL writes require explicit `--write`. Current `main` also validates BOE RSS through later RSS/coverage work. |
| `TASK-MCP-SOURCE-COVERAGE-001` | implemented locally | Read-only MCP tools expose registry coverage and existing RSS discovery JSONL without live fetches or writes. |
| `TASK-SOURCE-RSS-MONITOR-002` | implemented locally | BOE RSS and BOJA Atom feeds added as validated metadata-only discovery sources. |
| `TASK-SOURCE-COVERAGE-V1-SNAPSHOT-001` | implemented locally | Canonical Coverage v1 snapshot added from the executable registry and current monitor/MCP capabilities. |
| `TASK-SOURCE-BOPV-API-001` | implemented locally | Metadata-only BOPV REST/API discovery adapter added for the official Open Data Euskadi endpoint. |
| `TASK-SOURCE-COVERAGE-V1.1-SNAPSHOT-001` | implemented locally | Coverage v1.1 snapshot added after BOPV API discovery; MCP API JSONL exposure remains future work. |
| `TASK-MCP-API-DISCOVERY-OUTPUT-001` | implemented locally | Read-only MCP latest discovery reader now supports existing RSS and API monitor JSONL output. |
| `TASK-SOURCE-COVERAGE-INTEGRATION-CHECK-001` | implemented locally | Integration check validates registry, RSS monitor, API monitor, and MCP coverage/discovery together. |
| `TASK-MCP-COVERAGE-USAGE-DOCS-001` | implemented locally | Usage guide documents registry CLI, RSS/API discovery previews, MCP coverage tools, and safety boundaries. |
| `TASK-SOURCE-COVERAGE-SCHEDULE-001` | implemented locally | Controlled discovery run plan added; one source at a time, preview by default, explicit write only, report template included. |
| `TASK-SOURCE-RSS-MONITOR-003` | implemented locally | BOIB, BOC_CANTABRIA, and DOE RSS feeds added as validated metadata-only discovery sources; DOGC and BON were checked but not added. |
| `TASK-SOURCE-COVERAGE-V1.2-SNAPSHOT-001` | implemented locally | Coverage v1.2 snapshot added after RSS-003; current coverage is six RSS/Atom sources, one API discovery source, and no candidate/evidence expansion. |
| `TASK-DEV-CLI-ENTRYPOINT-CONSISTENCY-001` | implemented locally | Source-tree CLI validation now works through `python -m official_sources.cli`; docs explain stale console script handling and editable reinstall. |
| `TASK-SOURCE-OFFICIAL-DIRECTORY-001` | implemented locally | Reconciled `config/sources.yaml` against BOE/PAG official bulletin directories and added 43 provincial bulletin entries as `inventory_only` only. |
| `TASK-SOURCE-COVERAGE-V1.3-SNAPSHOT-001` | implemented locally | Historical coverage v1.3 snapshot added after official directory reconciliation; now superseded by the current 67-source baseline. |
| `TASK-SOURCE-PROVINCIAL-DISCOVERY-PILOT-001` | implemented locally | BOP_A_CORUNA HTML discovery pilot added as metadata-only, one-source monitoring; no candidates/evidence/PDFs/artifacts/downstream writes. |
| `TASK-SOURCE-COVERAGE-V1.4-SNAPSHOT-001` | implemented locally | Coverage v1.4 snapshot added after the BOP_A_CORUNA provincial HTML pilot; current coverage is 6 RSS/Atom, 1 API, and 1 HTML provincial discovery source. |
| `TASK-MCP-HTML-DISCOVERY-OUTPUT-001` | implemented locally | Read-only MCP latest discovery reader now supports existing HTML monitor JSONL output alongside RSS/API output. |
| `TASK-MCP-DISCOVERY-PREVIEW-001` | implemented locally | MCP now exposes controlled one-source metadata-only discovery previews for RSS/API/HTML, with no writes, candidates, evidence-grade records, PDFs/artifacts, backfills, or downstream writes. |
| `TASK-MCP-COVERAGE-RECOMMENDATIONS-001` | implemented locally | MCP now recommends next source work deterministically from registry/cache state, without LLMs, live fetches, previews, writes, candidates, evidence-grade records, or downstream writes. |
| `TASK-SOURCE-PROVINCIAL-DISCOVERY-002` | implemented locally | Evaluated BOP_ALBACETE, BOP_ALICANTE, and BOP_ALMERIA from MCP recommendations; added metadata-only HTML discovery for BOP_ALBACETE and BOP_ALICANTE; left BOP_ALMERIA inventory-only due ZK/JavaScript surface. |
| `TASK-SOURCE-COVERAGE-V1.5-SNAPSHOT-001` | implemented locally | Coverage v1.5 snapshot added after BOP_ALBACETE and BOP_ALICANTE HTML discovery promotion; current coverage is 6 RSS/Atom, 1 API, and 3 HTML provincial discovery sources. |
| `TASK-SOURCE-PROVINCIAL-PATTERN-REPORT-001` | implemented locally | Pattern report compares BOP_A_CORUNA, BOP_ALBACETE, and BOP_ALICANTE; decision is source-specific parsers for now with small shared helpers only. |
| `TASK-SOURCE-PROVINCIAL-HTML-HEALTH-001` | implemented locally | Health check validated BOP_A_CORUNA, BOP_ALBACETE, and BOP_ALICANTE previews without writes; all returned `records=1` with `not_candidate`, `not_evidence`, and `unclassified` safety status. |
| `TASK-VPS-INTEGRITY-CHECK-RAW-METADATA-001` | implemented locally | `integrity-check` now reports `local_path=NULL` metadata rows as `non_local_metadata`, while missing stored local artifact paths still fail. |
| `TASK-DOCS-RSS-MONITOR-STATE-RECONCILIATION-001` | merged | Reconciles RSS monitor documentation with current `main`; confirms RSS-001 should not be reopened and points next implementation work to RSS-004. |
| `TASK-SOURCE-RSS-MONITOR-004` | implemented locally | Adds BOC_CANARIAS, DOG, and BOP_LUGO as validated metadata-only RSS discovery sources; no writes, candidates, evidence-grade records, PDFs, downstream, VPS, Hermes, or systemd changes. |
| `TASK-SOURCE-RSS-MONITOR-005` | implemented locally | Adds BOCM and BOP_BADAJOZ as validated metadata-only RSS/Atom discovery sources. Atom entries without `published` now use `updated` as the primary item date. Live previews returned `records=1` for both sources with `not_candidate` and `not_evidence`; `data/rss_monitor` remained absent. No writes, candidates, evidence-grade records, PDFs/artifacts, downstream, VPS, Hermes, or systemd changes. |
| `TASK-SOURCE-COVERAGE-V1.6-SNAPSHOT-001` | implemented locally | Historical coverage snapshot after RSS-005, now superseded by the current 67-source baseline. `BOP_ALICANTE` was degraded/manual-review in that snapshot; no all-sources-green claim. Documentation report: `docs/reports/source-coverage-v1-6-snapshot-2026-05-29.md`. No source additions, monitor logic, writes, candidates, evidence, PDFs/artifacts, downstream, VPS, Hermes, or systemd changes. |
| `TASK-HERMES-AUDITOR-CANONICAL-ROOT-001` | completed | Hermes now audits `/opt/official-sources/app` through explicit `APP_ROOT`/`REPO_ROOT`/`TARGET_REPO` systemd environment; manual run returned `0/SUCCESS`, failed units are `0`, BOE/integrity/Hermes timers remain active, and report `vps-audit-20260527-044105.md` confirms RSS monitor present and RSS-001 completed. Follow-up scheduled validation is completed in `TASK-HERMES-AUDITOR-SCHEDULED-VALIDATION-001`. |
| `TASK-HERMES-AUDITOR-SCHEDULED-VALIDATION-001` | completed | VPS automatic Hermes run at `2026-05-28 00:07:44 UTC` generated `vps-audit-20260528-000744.md`; later VPS scheduled validation at `2026-05-28 00:45:01 UTC` generated `hermes-scheduled-validation-20260528-004501.md` with `verdict=GO`. The audited repo was `/opt/official-sources/app` on `main` commit `9ebf849f1663642071f60023385acc44c9fe5875`, worktree clean, Hermes service success exit 0, scheduled-validation service success exit 0, and `systemctl --failed` reported `0 loaded units listed`. Documentation report: `docs/reports/hermes-auditor-scheduled-validation-2026-05-28.md`. No BOE/integrity/RSS logic, timers, services, candidates, evidence, downstream writes, or VPS checkout fast-forward changes. |
| `TASK-HERMES-AUDITOR-RUNTIME-GUARD-001` | completed | VPS-only hardening added a non-blocking runner lock at `/var/lib/hermes-official-sources-auditor/hermes-auditor.lock` and a systemd drop-in with `TimeoutStartSec=5min`. Lock skip path returned status 0 with `skipped=concurrent_existing_run`; post-guard manual smoke generated `vps-audit-20260528-180136.md` against `/opt/official-sources/app` on `main` commit `08becb29162a8b896eedb91a7c8c33c4c8f6b79c`, clean worktree, RSS monitor present, service result success, and `systemctl --failed` reported `0 loaded units listed`. Documentation report: `docs/reports/hermes-auditor-runtime-guard-2026-05-28.md`. No timer calendar, model setting, source logic, downstream write, or checkout HEAD change. |
| `TASK-HERMES-DRIFT-AUDITOR-001` | completed | Adds read-only Hermes drift audit contract and evaluator: `config/hermes/audit_contract.yaml`, `src/official_sources/hermes_drift_audit.py`, `official-sources hermes audit`, tests, report examples, `docs/reports/hermes-drift-auditor-2026-06-03.md`, and post-merge checklist `docs/reports/hermes-drift-auditor-post-merge-checklist-2026-06-03.md`. PR #29 is merged in `main` at `9c672b3ca34fa795ae7fee5206a4247180ed6adb`; PR #31 externalized the release SHA; VPS release alignment completed on 2026-06-04 with worktree clean, `source_count=67`, `inventory_only=['DOUE']`, `systemctl --failed=0`, and Hermes strict audit `GO` with no failed gates or warnings. The live expected release SHA is read from `/etc/official-sources/hermes-audit-contract.yaml`, not from this in-repo queue. Stale VPS drift was preserved at `/var/tmp/official-sources-vps-drift-20260604-054135` and stashed as `pre-68dcffa-stale-vps-drift-preserved-20260604`; the stash was not popped. No reset, restore, unrelated deploy, systemd change, candidates, evidence-grade records, artifacts, downstream writes, or source expansion. |
| `TASK-HERMES-EXTERNAL-RELEASE-CONTRACT-002` | completed | Moves the approved release SHA out of the audited checkout. The in-repo `config/hermes/audit_contract.yaml` no longer requires `release.expected_head_sha`; `official-sources hermes audit` can load an external release contract through `--release-contract`, defaults the runtime path to `/etc/official-sources/hermes-audit-contract.yaml`, and supports `--strict-release-contract` for VPS mode. The VPS external contract is active at `/etc/official-sources/hermes-audit-contract.yaml`, owned `root:root` with mode `0644`. Hermes strict audit on VPS returned `GO`, failed gates `none`, warnings `none`; the live expected SHA must be read from the external contract because docs-only commits can advance `origin/main`. |
| `TASK-HERMES-SCHEDULED-STRICT-AUDIT-003` | implemented locally | Version-controls the scheduled Hermes strict audit path. Adds `official-sources hermes scheduled-audit`, `src/official_sources/hermes_scheduled_audit.py`, Hermes service/timer templates, and the deploy wrapper that calls the strict drift auditor with `/etc/official-sources/hermes-audit-contract.yaml`, `--strict-release-contract`, and `--fail-on-no-go`. The runner preserves watchdog evidence while propagating `NO-GO` as a non-zero service exit. Report: `docs/reports/hermes-scheduled-strict-audit-2026-06-04.md`. No VPS deploy, external contract update, source change, candidate/evidence write, downstream write, or live systemd mutation was performed in this implementation pass. |
| `TASK-MCP-DOWNSTREAM-PRODUCT-CONTRACTS-2026-06-04` | implemented locally | Adds `docs/architecture/downstream-integration-contracts.md` and `docs/reports/mcp-common-readiness-2026-06-04.md` to formalize the shared upstream/product-local boundary for `oposiciones2.0`, `eduayudas`, `la-ayuda`, `renta-verificable`, `subvenciones`, and `contratosabiertos`. Documents the current 67-source baseline, 67/67 candidate/evidence disabled invariants, read-only smoke interpretation, product matrix, forbidden actions, and first downstream tasks. No imports, downstream writes, live fetches, source changes, candidates, evidence-grade records, VPS, Hermes, systemd, or runtime mutations. |
| `TASK-EDUAYUDAS-OFFICIAL-SOURCES-REAL-PACKAGE-001` | implemented locally | Adds `docs/reports/eduayudas-real-package-readiness-2026-06-04.md` as a documentation-only readiness audit. Finds GO for a BOE-only evidence package preview and NO-GO for broad BDNS/BOE/autonomous ingestion because EduAyudas currently accepts only `BOE` in its official-sources evidence schema and migration. No downstream writes, imports, live fetches, source changes, candidates, evidence-grade records, aid drafts, publication, VPS, Hermes, systemd, or runtime mutations. |
| `TASK-MCP-SOURCE-RANKING-CLEANUP-001` | implemented locally | `recommend_next_sources` now excludes documented blocked/deferred provincial sources such as `BOP_ALMERIA` from the normal ranking while keeping them in registry inventory, and prioritizes Barcelona/Malaga then Bizkaia/Valencia/Sevilla/Zaragoza before alphabetical fallback. No monitors, scraping, writes, VPS, Hermes, timers, candidates, evidence, or downstream changes. |
| `TASK-PROVINCIAL-READONLY-BATCH-AUDIT-001` | implemented locally | Read-only batch audit classified the 38 remaining provincial `inventory_only` sources without documented blockers, using one landing-page request per source. Outputs are `docs/reports/provincial-readonly-batch-audit-2026-05-27.md` and `data/provincial_audit/provincial-readonly-batch-audit-2026-05-27.json`; recommended next pilots are Barcelona, Malaga, Bizkaia, Valencia, and Sevilla. No monitors, registry writes, scraping expansion, candidates, evidence, VPS, Hermes, timers, or downstream changes. |
| `TASK-PROVINCIAL-MONITORS-WAVE-001` | implemented locally | Adds metadata-only source-specific HTML discovery monitors for `BOP_BARCELONA` and `BOP_MALAGA`. Live preview validation on 2026-05-27 returned `records=1` for each with `not_candidate`, `not_evidence`, and `unclassified`; parser validation extracted 20 Barcelona and 19 Malaga current-page records. No PDF/artifact downloads, candidates, evidence-grade records, downstream writes, broad scraping/backfill, VPS, Hermes, systemd, or timer changes. |
| `TASK-PROVINCIAL-MONITORS-WAVE-002` | implemented locally | Adds metadata-only source-specific HTML discovery monitors for `BOP_BIZKAIA` and `BOP_VALENCIA`. Live preview validation on 2026-05-27 returned `records=1` for each with `not_candidate`, `not_evidence`, and `unclassified`; parser validation extracted 34 Bizkaia and 25 Valencia current-page records. No PDF/artifact downloads, candidates, evidence-grade records, downstream writes, broad scraping/backfill, VPS, Hermes, systemd, or timer changes. |
| `TASK-PROVINCIAL-MONITORS-HEALTH-001` | implemented locally | Documentation-only combined live preview health check for monitored provincial sources. Seven sources returned `records=1` in preview mode; `BOP_ALICANTE` failed twice with `httpx.ConnectError: [Errno 11002] getaddrinfo failed` for `sede.diputacionalicante.es`. Overall health decision is NO-GO until Alicante endpoint/DNS is reviewed. No writes, candidates, evidence-grade records, PDFs/artifacts, downstream, VPS, Hermes, systemd, or timer changes. |
| `TASK-BOP-ALICANTE-HEALTH-REGRESSION-001` | implemented locally | Read-only diagnostic found resolver-dependent DNS instability for `diputacionalicante.es`: normal monitor execution still fails with `getaddrinfo failed`, Cloudflare DNS-over-HTTPS returns `SERVFAIL`, Google DNS-over-HTTPS resolves `sede.diputacionalicante.es` to `195.53.69.137`, and the configured endpoint returns parser-compatible JSON when that official hostname is resolved to the Google DNS address. No safe monitor code fix was applied; `BOP_ALICANTE` remains degraded/manual-review for the combined healthy set. No registry status changes, writes, PDFs/artifacts, candidates, evidence-grade records, downstream, VPS, Hermes, systemd, or timer changes. |
| `TASK-BOP-ALICANTE-DEGRADED-DNS-001` | implemented locally | Formalizes `BOP_ALICANTE` as degraded/manual-review for provincial monitor health reporting due resolver-dependent DNS instability. The healthy monitored provincial set is now the seven passing sources; `BOP_ALICANTE` remains in registry as a monitored source but is excluded from healthy-set claims until normal DNS/live preview recovers. No `config/sources.yaml`, monitor code, registry status, writes, PDFs/artifacts, candidates, evidence-grade records, downstream, VPS, Hermes, systemd, or timer changes. |
| `TASK-SOURCE-COVERAGE-STATUS-CONTRACT-001` | merged | Adds `docs/SOURCE_STATUS_CONTRACT.md` as the downstream-facing contract for registry, monitor, discovery, candidate, evidence, and runtime-health states. Project operational status remains GO, downstream semantic ambiguity is closed by PR #21, no new source expansion is planned, writes remain disabled, and the current baseline keeps `candidate_creation_allowed=false` plus `evidence_grade_allowed=false` for 67/67 registered sources. `BOP_ALICANTE` remains degraded/manual-review; no runtime, parser, monitor, registry, VPS, Hermes, systemd, write, candidate, or evidence changes. |
| `TASK-PROJECT-CLOSURE-READONLY-UPSTREAM-001` | implemented locally | Marks `official-sources` as closed read-only upstream v1 after PR #21 merged the source status contract, local/VPS `main` were fast-forwarded, timers remained active, and old conflicting PRs #3, #7, and #16 were closed as superseded. Operational status remains GO; writes, candidate creation, and evidence-grade creation remain disabled; `BOP_ALICANTE` remains degraded/manual-review; coverage expansion remains frozen. |
| `TASK-MCP-DOWNSTREAM-SOURCE-NEEDS-MATRIX-001` | implemented locally | Adds `docs/MCP_DOWNSTREAM_SOURCE_NEEDS_MATRIX.md` to define downstream-demand classes and prioritize future MCP/source work for `oposiciones2.0`, `eduayudas`, `la-ayuda`, `renta-verificable`, and future consumers. Planning-only; no sources, parsers, monitors, writes, candidates, evidence-grade records, artifacts, downstream repos, VPS, Hermes, or systemd changes. |
| `TASK-MCP-OFFICIAL-SOURCES-CONTRACT-001` | implemented locally | Adds `docs/MCP_DOWNSTREAM_DEMAND_CONTRACT.md` as a specification-only contract for future consumer-aware MCP tools: `recommend_sources_for_consumer`, `discover_sources_for_case`, `build_evidence_packet`, `resolve_normative_reference`, and `resolve_fiscal_reference`. No tools were implemented and no runtime behavior changed. |
| `TASK-MCP-TOOLS-READONLY-SKELETON-001` | implemented locally | Implements read-only MCP tool `recommend_sources_for_consumer`, backed by deterministic downstream-demand profiles. The tool returns source status and safety envelopes for known consumers without live fetches, previews, writes, candidates, evidence-grade records, artifacts, registry mutation, or downstream repository writes. |
| `TASK-SOURCE-COVERAGE-REOPEN-OPOSICIONES-001` | implemented locally | Adds metadata-only HTML discovery monitors for `BOP_CASTELLON` and `BOP_SEVILLA`, scoped to `oposiciones2.0` public-employment alert demand. Its registry snapshot is historical and is superseded by the current 67-source baseline. No candidates, evidence-grade records, PDF/artifact downloads, downstream writes, broad backfills, VPS, Hermes, systemd, or `BOP_ALICANTE` runtime-status changes. |
| `TASK-MCP-CASE-TAXONOMY-001` | implemented locally | Adds `docs/MCP_CASE_TAXONOMY.md`, example taxonomy output, and read-only MCP tool `list_case_taxonomy` for stable demand classes, case types, topics, jurisdictions, safe outputs, and `must_not_infer` values. No source fetches, monitor previews, JSONL writes, registry mutation, candidates, evidence-grade records, downstream writes, product approval, legal/fiscal decisions, or `BOP_ALICANTE` status changes. |
| `TASK-MCP-EVIDENCE-PACKETS-EDUAYUDAS-001` | implemented locally | Adds `docs/MCP_EDUAYUDAS_EVIDENCE_PACKET_PROFILE.md`, example output, and read-only MCP tool `build_evidence_packet` for review-only education-aid packet requirements. Prioritizes BDNS, BOE, BOJA, BOCYL, BOCM, and DOGV; no live fetches, artifacts, aid creation, candidates, evidence-grade records, eligibility decisions, product approval, downstream writes, VPS, Hermes, systemd, or `BOP_ALICANTE` changes. |
| `TASK-MCP-BENEFIT-SOURCE-RESOLVER-LAAYUDA-001` | implemented locally | Adds `docs/MCP_LAAYUDA_SOURCE_RESOLVER_PROFILE.md`, example output, and read-only MCP tool `resolve_normative_reference` for manual-review source leads. No arbitrary URL fetching, invented URLs, Markdown generation, legal meaning, eligibility, amount/deadline verification, product publication, downstream writes, VPS, Hermes, systemd, or `BOP_ALICANTE` changes. |
| `TASK-MCP-FISCAL-REFERENCE-RESOLVER-RENTA-001` | implemented locally | Adds `docs/MCP_RENTA_FISCAL_REFERENCE_PROFILE.md`, example output, and read-only MCP tool `resolve_fiscal_reference` for AEAT-first manual-review fiscal source leads. No tax advice, deduction applicability, legal meaning, fiscal claim verification, product publication, downstream writes, candidates, evidence-grade records, artifacts, live fetches, VPS, Hermes, systemd, or `BOP_ALICANTE` changes. |
| `TASK-MCP-DOWNSTREAM-INTEGRATION-CLOSURE-001` | implemented locally | Adds `docs/MCP_DOWNSTREAM_INTEGRATION_CLOSURE.md`, example output, and read-only MCP tool `list_downstream_integration_smokes` for the current consumer smoke matrix covering `oposiciones2.0`, `eduayudas`, `la-ayuda`, and `renta-verificable`. No smoke execution, downstream preview/import execution, live fetches, JSONL writes, registry mutation, candidates, evidence-grade records, product writes, publication, notifications, VPS, Hermes, systemd, or `BOP_ALICANTE` changes. |
| `TASK-MCP-DOWNSTREAM-SMOKE-CHECKER-001` | implemented locally | Adds read-only MCP tool `check_downstream_integration_smokes` to execute only hardcoded in-process `official-sources` MCP/planner calls from the smoke matrix and check expected statuses, expected output fields, and safety envelopes. No downstream preview/import commands, shell execution, arbitrary tool input, monitor previews, live fetches, JSONL writes, registry mutation, candidates, evidence-grade records, product writes, publication, notifications, VPS, Hermes, systemd, or `BOP_ALICANTE` changes. |
| `TASK-MCP-READONLY-UPSTREAM-V1-FINAL-CLOSURE-001` | implemented locally | Adds `docs/MCP_READONLY_UPSTREAM_V1_FINAL_CLOSURE.md` and final validation notes. Marks `official-sources` closed as read-only upstream v1 for current consumers; downstream acceptance moves to product-side preview/import smokes. No source expansion, parser changes, monitor changes, live fetches, runtime data writes, candidates, evidence-grade records, downstream writes, VPS, Hermes, systemd, or `BOP_ALICANTE` changes. |
| `TASK-PROJECT-STABLE-CLOSEOUT-2026-06-01` | superseded by `REL-001` | Earlier closeout snapshot before relay work: 65 registered sources, 50 `monitor_validated`, 9 `metadata_adapter_validated`, and 6 `inventory_only`. Its `REL-001` deferred stance is superseded by the relay implementation below. |
| `TASK-AYTO-ZARAGOZA-EMPLEO-001` | implemented locally | Adds `AYTO_ZARAGOZA_EMPLEO` as a local metadata-only API monitor for the official Zaragoza municipal public-employment JSON endpoint. `BOP_ZARAGOZA` remains separate and still blocked for provincial BOP runtime coverage. |
| `REL-001` | implemented locally and on VPS | Adds allowlisted relay implementations and promotes `BOP_CUENCA`, `BOP_SALAMANCA`, and `BOP_ZARAGOZA` to metadata-only HTML monitors through the steamcases-vps IONOS relay. Current registry: 66 sources, 54 `monitor_validated`, 9 `metadata_adapter_validated`, 3 `inventory_only`, and 43/43 provincial BOP monitors. No arbitrary URL relay, PDFs/artifacts, candidates, evidence-grade records, broad backfill, downstream writes, or product automation. |
| `TASK-BOP-ALICANTE-RECOVERY-2026-06-02` | implemented locally and on VPS | Documents recovery from the earlier resolver-dependent DNS degradation. Local and VPS checks resolve `sede.diputacionalicante.es` to `195.53.69.137`, the landing returns `HTTP 200`, and preview for `2026-05-27` returns `records=1` with `not_candidate` and `not_evidence`. `config/sources.yaml` now records `degraded=false`, `degraded_reason=null`, and a recovery note. DNS-dependent residual risk remains documented; no candidates, evidence-grade records, PDFs/artifacts, downstream writes, product automation, Hermes, systemd, or timer changes. |
| `TASK-BOME-BOCCE-HTML-MONITORS-2026-06-02` | implemented locally | Promotes `BOME` and `BOCCE` from `inventory_only` to metadata-only HTML monitors. BOME follows the official landing page to the matching issue page and stores article HTML URLs; BOCCE stores issue-level official URLs with `pdf_endpoint_not_downloaded`. Current registry: 67 sources, 56 `monitor_validated`, 10 `metadata_adapter_validated`, 1 `inventory_only` (`DOUE`). No PDFs/artifacts, candidates, evidence-grade records, broad backfill, downstream writes, VPS, Hermes, systemd, or timer changes. |

## Next

| Task | Status | Boundary |
| --- | --- | --- |
| `TASK-HERMES-FAIL-CLOSED-SOURCE-FRESHNESS-001` | implemented locally | Adds `docs/architecture/hermes-fail-closed-source-freshness-contract.md` as a contract-only definition for future Hermes source freshness SLOs, GO/WARNING/NO-GO verdicts, read-only downstream smokes, report format, and escalation rules. No runtime implementation, source changes, registry mutation, SQLite writes, monitor materialization, candidates, evidence, artifacts, downstream writes, VPS, Hermes systemd, or deploy changes. |
| `TASK-HERMES-FRESHNESS-REPORT-SKELETON-001` | implemented locally | Adds a pure Hermes freshness report skeleton and CLI entrypoint for local JSON state fixtures. It calculates GO/WARNING/NO-GO, renders source, last_seen, threshold_hours, age_hours, calendar_exception, impact, and forbidden-action flags, and writes only the explicit report path when `--output` is provided. No systemd, registry, `ingest-monitor-date`, SQLite, official_documents, evidence, candidates, downstream smokes, automatic issues, timers, VPS, or deploy changes. |
| `TASK-HERMES-FRESHNESS-REPORT-REAL-INPUTS-001` | implemented locally | Extends `official-sources hermes freshness-report` with a read-only runtime input adaptor over existing `data/rss_monitor`, `data/api_monitor`, and `data/html_monitor` JSONL outputs while preserving the JSON fixture path. It maps observed discovery timestamps and expected missing sources into the existing freshness engine. No semantic change to GO/WARNING/NO-GO, systemd, timers, registry mutation, `ingest-monitor-date`, SQLite writes, official_documents, evidence, candidates, downstream smokes, automatic issues, VPS, or deploy changes. |
| `TASK-HERMES-FRESHNESS-REPORT-RUNTIME-SMOKE-001` | implemented locally | Runs the freshness report against the local runtime root with expected critical sources `BOE`, `BDNS`, and `BOCM`. The checkout contains zero `data/*_monitor/*/*_discovery.jsonl` files, so the report correctly returns `NO-GO` with explicit `missing` reasons rather than an empty `GO`. Output: `docs/reports/hermes-freshness-runtime-smoke.md`. No systemd, timers, VPS, SQLite, registry mutation, `ingest-monitor-date`, downstream smokes, automatic issues, candidates, evidence, publication, or product writes. |
| `TASK-HERMES-FRESHNESS-SCHEDULE-POLICY-001` | implemented locally | Adds `docs/architecture/hermes-freshness-schedule-policy.md` to define future report-only scheduling policy, report path, exit-code behavior, retention, operational verdict rules, and VPS pre-activation checklist for the freshness report. No systemd/timer activation, VPS deployment, downstream smokes, SQLite writes, `ingest-monitor-date`, registry mutation, official_documents, evidence, candidates, publication, automatic issues, or strict-audit behavior changes. |
| `TASK-HERMES-FRESHNESS-REPORT-ONLY-INTEGRATION-001` | implemented locally | Adds `official-sources hermes scheduled-freshness-report` and `src/official_sources/hermes_scheduled_freshness_report.py` as a report-only wrapper that runs the existing freshness report with `--runtime-root .` and writes one deterministic markdown report under `<state-root>/freshness-reports/`. A generated `VERDICT: NO-GO` remains report content, not a hard process gate. No systemd/timer activation, VPS deployment, git pull, `ingest-monitor-date`, SQLite writes, registry mutation, official_documents, evidence, candidates, publication, downstream smokes, automatic issues, retention deletion, or strict-audit behavior changes. |
| `TASK-HERMES-FRESHNESS-VPS-MANUAL-SMOKE-001` | implemented locally and on VPS | Documents the first VPS manual smoke for `official-sources hermes scheduled-freshness-report`. The VPS checkout was clean and fast-forwarded from `506b68b` to `912b89f` with the external strict-audit release contract updated to the same SHA. The wrapper generated `/var/lib/hermes-official-sources-auditor/freshness-reports/hermes-freshness-report-20260613-174731.md`, returned exit 0, and the report body correctly returned `VERDICT: NO-GO` for missing critical freshness state (`BOE`, `BDNS`, `BOCM`). Post-smoke strict audit returned `GO`, worktree stayed clean, and `systemctl --failed` remained 0. No systemd/timer activation, service modification/restart, downstream smoke, SQLite write, `ingest-monitor-date`, registry mutation, official_documents, evidence, candidates, publication, or automatic issue creation. Report: `docs/reports/hermes-freshness-vps-manual-smoke-2026-06-13.md`. |
| `TASK-HERMES-FRESHNESS-OBSERVATION-SOURCE-MAPPING-001` | implemented locally | Adds `docs/architecture/hermes-freshness-observation-source-mapping.md` to map critical freshness observations for `BOE`, `BDNS`, and `BOCM`. The mapping confirms that current local/VPS runtime has no monitor JSONL, no ingestion_runs, and no official_documents for the critical sources, so the current `NO-GO` is correct. BOE/BOCM should prefer RSS monitor JSONL `discovered_at`; BDNS needs a read-only SQLite ingestion_runs adaptor or separate observation producer because generic API monitor does not support BDNS. No scheduler activation, systemd/timer changes, live fetches, SQLite writes, `ingest-monitor-date`, registry mutation, official_documents, evidence, candidates, publication, downstream smokes, or automatic issues. |
| `TASK-HERMES-FRESHNESS-OBSERVATION-PRODUCER-001` | implemented locally | Adds `official-sources hermes freshness-observations` and `src/official_sources/hermes_freshness_observation_producer.py` to derive operational freshness observation JSONL from existing monitor outputs and optional read-only SQLite `ingestion_runs`. Monitor observations use `discovered_at`/`observed_at` only; `published_at`/`updated_at` remain diagnostic `latest_record_date` values, not `observed_at`. The command writes only the explicit `--output` JSONL and does not activate scheduler/systemd/timers, run live fetches, execute `ingest-monitor-date`, write SQLite, mutate registry, create official_documents/evidence/candidates/publication, perform product/downstream writes, or open automatic issues. |
| `TASK-HERMES-FRESHNESS-OBSERVATION-PRODUCER-SMOKE-001` | implemented locally and on VPS | Documents local and VPS manual smoke for `official-sources hermes freshness-observations` after PR #46 merge. Local smoke on `69762bf` returned exit 2 with `no freshness observations found` and created no JSONL, confirming fail-closed behavior. VPS was fast-forwarded from `912b89f` to `69762bf`, the external strict-audit contract was aligned to the same SHA, and the producer smoke against `/opt/official-sources/app` plus read-only `official-sources.sqlite` also returned exit 2 with no JSONL. Runtime evidence: monitor discovery JSONL count is 0 and SQLite `ingestion_runs` for `BOE`/`BDNS`/`BOCM` is empty. Post-smoke strict audit returned `GO`, worktree stayed clean, and `systemctl --failed` remained 0. Scheduler remains blocked. Report: `docs/reports/hermes-freshness-observation-producer-smoke-2026-06-13.md`. |
| `TASK-HERMES-FRESHNESS-CRITICAL-SOURCE-OBSERVATIONS-001` | implemented locally and smoke-tested on VPS | Adds read-only `--observations-jsonl` support to `official-sources hermes freshness-report` so producer JSONL can feed the freshness decision engine without monitor dirs or live fetches. VPS inspection found the earlier smoke used the checkout-local SQLite, while the real service DB is `/opt/official-sources/data/official_sources.sqlite`. Running the existing producer against that real DB produced three operational observations: `BOE` healthy at `2026-06-13T07:41:10.656169Z`, `BDNS` stale at `2026-05-31T13:47:15.960144Z`, and `BOCM` stale at `2026-05-21T20:19:13.900362Z`. The freshness report now returns `NO-GO` for stale `BDNS`/`BOCM`, not missing inputs. No scheduler activation, systemd/timer changes, live fetches, `ingest-monitor-date`, SQLite writes, registry mutation, official_documents/evidence/candidates/publication, product/downstream writes, or automatic issues. Report: `docs/reports/hermes-critical-source-observations-2026-06-13.md`. |
| `TASK-HERMES-FRESHNESS-OBSERVATION-JSONL-VPS-SMOKE-001` | implemented on VPS | Documents the post-merge VPS smoke for PR #48 on `6b689a7`. The VPS checkout and external strict-audit contract were aligned to `6b689a72b0b313192639af32a1facec538ada495`. Running `freshness-observations` against the real service DB `/opt/official-sources/data/official_sources.sqlite` wrote 3 observations under `/tmp`, and `freshness-report --observations-jsonl` returned exit 0 with `VERDICT: NO-GO`: `BOE` healthy (`age=12.0h`), `BDNS` stale (`age=317.9h`), and `BOCM` stale (`age=551.4h`). Post-smoke strict audit returned `GO`, worktree stayed clean, and `systemctl --failed` remained 0. Scheduler remains blocked; next task should investigate stale `BDNS`/`BOCM` observations. Report: `docs/reports/hermes-freshness-observation-jsonl-vps-smoke-2026-06-13.md`. |
| `TASK-HERMES-FRESHNESS-STALE-SOURCES-INVESTIGATION-001` | implemented on VPS | Documents a read-only VPS investigation of stale `BDNS` and `BOCM` freshness observations. Systemd has active BOE, integrity, and Hermes timers, but no BDNS/BOCM refresh units. The real service DB shows exact source codes and stale operational observations: latest `BDNS` success at `2026-05-31T13:47:15.960144+00:00`, latest accepted `BOCM` observation at `2026-05-21T20:19:13.900362+00:00`, and later BOCM timeout failures. Runtime paths contain no current BDNS/BOCM monitor JSONL. Post-inspection strict audit had no failed gates but warned that remote `main` advanced to docs-only PR #49 while the VPS release contract stayed on `6b689a7`; no VPS fast-forward was performed in this read-only task. Scheduler remains blocked. Report: `docs/reports/hermes-freshness-stale-sources-investigation-2026-06-13.md`. |
| `TASK-HERMES-FRESHNESS-BDNS-BOCM-OBSERVATION-DESIGN-001` | implemented locally | Adds `docs/architecture/hermes-freshness-bdns-bocm-observation-design.md` as a design-only contract for real BDNS/BOCM observation paths. The design keeps Hermes as consumer/reporter, recommends BOCM RSS monitor JSONL under `/var/lib/hermes-official-sources-auditor/freshness-runtime`, keeps BDNS ingestion as operator-owned state rather than a Hermes auto-fix, and requires a future BDNS observation-only producer if daily BDNS freshness is needed. It also rejects publication dates as freshness timestamps, keeps generated monitor state outside the audited checkout, blocks scheduler activation, and forbids systemd/timer changes, live fetches in this PR, SQLite writes, registry mutation, `official_documents`/evidence/candidates/publication, downstream writes, and automatic issues. |
| `TASK-HERMES-FRESHNESS-BOCM-RSS-OBSERVATION-001` | implemented locally | Adds manual CLI wrapper `official-sources hermes bocm-rss-observation` and `src/official_sources/hermes_bocm_rss_observation.py`. The wrapper runs the existing BOCM RSS monitor for one source/date into `<state-root>/freshness-runtime/data/rss_monitor`, then runs `freshness-observations` for `BOCM` into `<state-root>/freshness-observations/latest-bocm-rss.jsonl`. It fails if either subprocess fails or expected output is missing. No scheduler activation, systemd/timer changes, SQLite writes, registry mutation, `ingest-monitor-date`, `ingest-bocm-date`, `official_documents`/evidence/candidates/publication, product/external project writes, automatic issues, or BDNS changes. Scheduler remains blocked pending BDNS observation path and local/VPS smoke. |
| `TASK-HERMES-FRESHNESS-BOCM-RSS-OBSERVATION-SMOKE-001` | implemented locally and on VPS | Documents local and VPS smoke after PR #52 merge. Local temp-state smoke and VPS real-state smoke both returned `bocm_rss_observation_exit_code=0`, produced BOCM RSS JSONL plus `latest-bocm-rss.jsonl`, and `freshness-report --observations-jsonl` returned `VERDICT: GO` for BOCM with `observed_at=2026-06-13T00:00:00Z` and `timestamp_type=observed`. VPS files were written as `official-sources:official-sources`; post-smoke strict audit returned `GO`, worktree stayed clean, and `systemctl --failed` remained 0. Scheduler remains blocked because BDNS still lacks an approved observation-only path. Report: `docs/reports/hermes-bocm-rss-observation-smoke-2026-06-13.md`. |
| `TASK-HERMES-FRESHNESS-BDNS-OBSERVATION-ONLY-001` | implemented locally | Adds manual CLI wrapper `official-sources hermes bdns-observation` and `src/official_sources/hermes_bdns_observation.py`. The wrapper performs one bounded BDNS latest API observation into `<state-root>/freshness-runtime/data/api_monitor/BDNS/<YYYY-MM-DD>/api_discovery.jsonl`, then runs `freshness-observations` for `BDNS` into `<state-root>/freshness-observations/latest-bdns.jsonl`. `observed_at` comes from the operator-controlled observation time; BDNS `fechaPublicacion` is kept only as diagnostic `published_at`/`latest_record_date`. No scheduler activation, systemd/timer changes, `ingest-bdns-*`, SQLite writes, registry mutation, `official_documents`/evidence/candidates/publication, product/external project writes, or automatic issues. Scheduler remains blocked pending local/VPS smoke. |
| `TASK-HERMES-FRESHNESS-CRITICAL-SOURCES-LOCAL-SMOKE-001` | implemented locally | Documents the first local critical-source smoke after BDNS observation support. BOE, BDNS, and BOCM all report `healthy` and the local freshness report returns `VERDICT: GO` from temp-state observations. The smoke found and fixes a real parser gap: BOE RSS diagnostic `published_at` may be RFC 2822, so `freshness-observations` now accepts RFC 2822 record-date diagnostics while still using only `discovered_at`/`observed_at` for freshness `observed_at`. No scheduler activation, VPS mutation, systemd/timer changes, SQLite writes, `ingest-bdns-*`, `ingest-monitor-date`, registry mutation, `official_documents`/evidence/candidates/publication, product writes, or automatic issues. Scheduler remains blocked pending VPS smoke after merge. Report: `docs/reports/hermes-critical-sources-local-smoke-2026-06-14.md`. |
| `TASK-HERMES-FRESHNESS-CRITICAL-SOURCES-VPS-SMOKE-001` | implemented on VPS | Documents the real VPS critical-source smoke after #54/#55. VPS checkout and external strict-audit contract were aligned to `afc4d3781838b58cb6a1ed9af873e6574cc62590`. BOE RSS, BOCM RSS observation, and BDNS observation all exited 0; `freshness-observations` wrote 3 observations to `/var/lib/hermes-official-sources-auditor/freshness-observations/latest-critical.jsonl`; `freshness-report` returned `VERDICT: GO` with BOE/BDNS/BOCM all healthy. A narrow state permission fix changed `freshness-reports/` owner from `root:root` to `official-sources:official-sources` so the service user can write report-only smoke files. Post-smoke strict audit returned `GO`, `systemctl --failed` returned 0, and the VPS worktree stayed clean. No scheduler activation, systemd/timer changes, SQLite writes, `ingest-bdns-*`, `ingest-monitor-date`, registry mutation, `official_documents`/evidence/candidates/publication, product writes, or automatic issues. Report: `docs/reports/hermes-critical-sources-vps-smoke-2026-06-14.md`. |
| `TASK-HERMES-FRESHNESS-REPORT-ONLY-SCHEDULE-INTEGRATION-001` | implemented locally | Adds a service-only report runner for critical-source freshness. `official-sources hermes scheduled-freshness-report` now performs BOE RSS, BOCM RSS observation, BDNS observation, `freshness-observations`, and `freshness-report --observations-jsonl` into the Hermes state root. Adds `deploy/systemd/official-sources-hermes-freshness-report.service` and `run-official-sources-hermes-freshness-report.sh` but deliberately adds no timer. Allowed writes are limited to `freshness-runtime`, `freshness-observations/latest-critical.jsonl`, and `freshness-reports`. No scheduler activation, timer creation, git pull, registry mutation, SQLite writes, `ingest-bdns-*`, `ingest-monitor-date`, strict-audit gate changes, `official_documents`/evidence/candidates/publication, downstream smokes, product writes, or automatic issues. Next required step is manual VPS service smoke before any timer task. |
| `TASK-HERMES-FRESHNESS-SERVICE-MANUAL-SMOKE-001` | implemented on VPS | Documents the manual VPS smoke after PR #57 merge. VPS was fast-forwarded from `abd7a4f` to `3320f44`, the external strict-audit contract was aligned to `3320f44a6c4b7f1d73084b67905e0ff3872a4ebf`, and only the report-only service/wrapper were installed. `systemctl start official-sources-hermes-freshness-report.service` returned exit 0; the service wrote `/var/lib/hermes-official-sources-auditor/freshness-observations/latest-critical.jsonl` and `/var/lib/hermes-official-sources-auditor/freshness-reports/hermes-freshness-report-20260614-043642.md`; the report returned `VERDICT: GO` with BOE/BDNS/BOCM healthy. Post-smoke strict audit returned `GO`, `systemctl --failed` returned 0, and the VPS worktree stayed clean. No timer was added/enabled, no git pull from service, no registry mutation, SQLite writes, `ingest-bdns-*`, `ingest-monitor-date`, strict-audit gate change, `official_documents`/evidence/candidates/publication, downstream writes, or auto-fix. Report: `docs/reports/hermes-freshness-service-manual-smoke-2026-06-14.md`. |
| `TASK-HERMES-FRESHNESS-REPORT-ONLY-TIMER-001` | implemented locally | Adds `deploy/systemd/official-sources-hermes-freshness-report.timer` to schedule the already validated report-only service at `08:15 UTC`, after BOE daily and before integrity checks, with `Persistent=true` and `RandomizedDelaySec=5m`. Adds `docs/architecture/hermes-freshness-report-only-timer.md` with explicit manual VPS activation and first scheduled-run validation steps. No service logic changes, timer activation, auto-fix, git pull, registry mutation, SQLite writes, `ingest-bdns-*`, `ingest-monitor-date`, strict-audit gate changes, `official_documents`/evidence/candidates/publication, downstream writes, or product writes. |
| `TASK-HERMES-FRESHNESS-TIMER-ACTIVATION-SMOKE-001` | implemented on VPS | Documents activation of `official-sources-hermes-freshness-report.timer` after PR #59 merge. VPS was fast-forwarded from `b42baa7` to `319deb0`; the timer was installed, enabled, and observed `active (waiting)` with next trigger `2026-06-14 08:19:28 UTC`. Manual post-activation service validation returned exit 0, wrote `latest-critical.jsonl` and `hermes-freshness-report-20260614-044301.md`, and the report returned `VERDICT: GO` with BOE/BDNS/BOCM healthy. An initial strict audit exposed a one-character contract transcription error (`319deb068...` vs actual `319deb05...`); after correcting `/etc/official-sources/hermes-audit-contract.yaml` to `319deb05f97f6211111a50be91af4dec672b9062`, strict audit returned `GO`, `systemctl --failed` returned 0, and the VPS worktree stayed clean. No auto-fix, git pull from timer/service, registry mutation, SQLite writes, `ingest-bdns-*`, `ingest-monitor-date`, strict-audit gate change, `official_documents`/evidence/candidates/publication, downstream writes, or product writes. First scheduled run validation remains pending. Report: `docs/reports/hermes-freshness-timer-activation-smoke-2026-06-14.md`. |
| `TASK-PROVINCIAL-MONITORS-WAVE-003` | deferred | No new source expansion is currently planned. May proceed only if a future task explicitly reopens source expansion with scoped runtime checks. `BOP_ALICANTE` is no longer currently degraded after the 2026-06-02 recovery, but its DNS-dependent residual risk must remain documented. |
| `REL-001-CUENCA-ZARAGOZA-EGRESS` | completed by `REL-001` | steamcases-vps / IONOS AS8560 validates the relay path for `BOP_CUENCA`, `BOP_SALAMANCA`, and `BOP_ZARAGOZA`; Cloudflare Worker, Smart Placement, and Vercel regions remained documented failed attempts. |
| `TASK-SOURCE-PROVINCIAL-URL-DIFF-AUDIT-001` | proposed | Compare BOE and PAG provincial URLs source by source; documentation-only unless a URL correction is verified. |
| `TASK-SOURCE-HTML-MONITOR-PILOT-001` | proposed | Only for sources without RSS/API, after source-specific endpoint/robots/fixture audit. |
| `TASK-SOURCE-COVERAGE-RUN-REPORT-001` | proposed | Only if actual metadata-only JSONL writes are run; document source, date, output path, row count, and guardrails. |
| `TASK-MCP-DISCOVERY-OUTPUT-SAMPLES-001` | proposed | Generate safe sample discovery outputs only if needed; avoid live writes unless explicitly scoped. |
| `TASK-SOURCE-PROVINCIAL-DISCOVERY-003` | proposed | Evaluate at most 2 more provincial sources using the pattern report and health-check criteria; no bulk monitoring and no generic framework unless evidence improves. |
| `TASK-BOP-VALLADOLID-001` | implemented locally | Promotes `BOP_VALLADOLID` from inventory-only to metadata-only HTML monitor using the official date-scoped Liferay URL pattern confirmed by external repo review. No PDFs/artifacts, candidates, evidence-grade records, broad backfill, downstream writes, VPS, Hermes, systemd, or timer changes. |
| `TASK-BOP-PONTEVEDRA-001` | implemented locally | Promotes `BOP_PONTEVEDRA` from inventory-only to metadata-only HTML monitor using the official date-scoped BOPPO detail URL. No PDFs/artifacts, candidates, evidence-grade records, broad backfill, downstream writes, VPS, Hermes, systemd, or timer changes. |
| `TASK-BOP-AVILA-001` | implemented locally | Promotes `BOP_AVILA` from inventory-only to metadata-only HTML monitor using the official date-scoped bulletin page. The monitor stores announcement PDF URLs as official metadata only and marks `pdf_endpoint_not_downloaded`; no PDFs/artifacts, candidates, evidence-grade records, broad backfill, downstream writes, VPS, Hermes, systemd, or timer changes. |
| `TASK-BOP-CORDOBA-001` | implemented locally | Promotes `BOP_CORDOBA` from inventory-only to metadata-only HTML monitor using the official date-scoped Next.js page. The parser reads server-returned announcement payload metadata and marks `pdf_endpoint_not_downloaded`; no PDFs/artifacts, candidates, evidence-grade records, broad backfill, downstream writes, VPS, Hermes, systemd, or timer changes. |
| `TASK-BOP-GRANADA-001` | implemented locally | Promotes `BOP_GRANADA` from inventory-only to metadata-only HTML monitor using the official date-scoped public BOP page. The monitor stores public announcement detail URLs as official metadata; no PDFs/artifacts, candidates, evidence-grade records, broad backfill, downstream writes, VPS, Hermes, systemd, or timer changes. |
| `TASK-BOP-SORIA-001` | implemented locally | Promotes `BOP_SORIA` from inventory-only to metadata-only HTML monitor using the official date-scoped bulletin page and its public detail link. The monitor stores document download URLs as official metadata only and marks `pdf_endpoint_not_downloaded`; no PDFs/artifacts, candidates, evidence-grade records, broad backfill, downstream writes, VPS, Hermes, systemd, or timer changes. |
| `TASK-BOR-001` | implemented locally | Promotes `BOR` from inventory-only to metadata-only API monitor using the official XML servlet. The monitor resolves the issue number from the monthly calendar and fetches one daily summary. No PDFs/artifacts, candidates, evidence-grade records, broad backfill, downstream writes, VPS, Hermes, systemd, or timer changes. |
| `TASK-DOCM-001` | implemented locally | Promotes `DOCM` from inventory-only to metadata-only HTML monitor using the official date-scoped summary page. The monitor stores HTML document URLs and ignores visible PDF download links; no PDFs/artifacts, candidates, evidence-grade records, broad backfill, downstream writes, VPS, Hermes, systemd, or timer changes. |
| `TASK-BON-001` | implemented locally | Promotes `BON` from inventory-only to metadata-only HTML monitor using the official monthly index and issue summary pages. No PDFs/artifacts, candidates, evidence-grade records, broad backfill, downstream writes, VPS, Hermes, systemd, or timer changes. |
| `TASK-BOP-HUELVA-001` | implemented locally | Promotes `BOP_HUELVA` from inventory-only to metadata-only API monitor using the official BOP AJAX endpoint with `tipo=2` and one requested date. No PDFs/artifacts, candidates, evidence-grade records, broad backfill, downstream writes, VPS, Hermes, systemd, or timer changes. |
| `TASK-BOP-PALENCIA-001` | implemented locally | Promotes `BOP_PALENCIA` from inventory-only to metadata-only HTML monitor using the official latest bulletin listing with requested-date verification from the bulletin PDF basename. It emits bulletin-level metadata only; no PDFs/artifacts, candidates, evidence-grade records, broad backfill, downstream writes, VPS, Hermes, systemd, or timer changes. |
| `TASK-BOPA-001` | implemented locally | Promotes `BOPA` from inventory-only to metadata-only HTML monitor using the official date-scoped BOPA summary. No PDFs/artifacts, candidates, evidence-grade records, broad backfill, downstream writes, VPS, Hermes, systemd, or timer changes. |
| `TASK-BOP-JAEN-001` | implemented locally | Promotes `BOP_JAEN` from inventory-only to metadata-only HTML monitor using the official date-scoped BOP page. The monitor stores PDF endpoints as official metadata only and marks `pdf_endpoint_not_downloaded`; no PDFs/artifacts, candidates, evidence-grade records, broad backfill, downstream writes, VPS, Hermes, systemd, or timer changes. |
| `TASK-BOP-LLEIDA-001` | implemented locally | Promotes `BOP_LLEIDA` from inventory-only to metadata-only HTML monitor using the official latest bulletin listing with requested-date verification. No PDFs/artifacts, candidates, evidence-grade records, broad backfill, downstream writes, VPS, Hermes, systemd, or timer changes. |
| `TASK-BOP-ZAMORA-001` | implemented locally | Promotes `BOP_ZAMORA` from inventory-only to metadata-only HTML monitor using the official listing page and matching-date detail page. No PDFs/artifacts, candidates, evidence-grade records, broad backfill, downstream writes, VPS, Hermes, systemd, or timer changes. |
| `TASK-BOP-BURGOS-001` | implemented locally | Promotes `BOP_BURGOS` from inventory-only to metadata-only HTML monitor using the official date-scoped archive and bulletin detail page. The monitor stores announcement PDF endpoints as official metadata only and marks `pdf_endpoint_not_downloaded`; no PDFs/artifacts, candidates, evidence-grade records, broad backfill, downstream writes, VPS, Hermes, systemd, or timer changes. |
| `TASK-BOP-LEON-001` | implemented locally | Promotes `BOP_LEON` from inventory-only to metadata-only bulletin-level HTML monitor using the official date-scoped bulletin page. Granular announcement HTML was not confirmed; no PDFs/artifacts, candidates, evidence-grade records, broad backfill, downstream writes, VPS, Hermes, systemd, or timer changes. |
| `TASK-BOP-SEGOVIA-001` | implemented locally | Promotes `BOP_SEGOVIA` from inventory-only to metadata-only bulletin-level HTML monitor using the official landing page cards with requested-date verification. No PDFs/artifacts, candidates, evidence-grade records, broad backfill, downstream writes, VPS, Hermes, systemd, or timer changes. |
| `TASK-BOP-TOLEDO-001` | implemented locally | Promotes `BOP_TOLEDO` from inventory-only to metadata-only HTML monitor using the official date-scoped summary page. The monitor stores DocGet PDF endpoints as official metadata only and marks `pdf_endpoint_not_downloaded`; no PDFs/artifacts, candidates, evidence-grade records, broad backfill, downstream writes, VPS, Hermes, systemd, or timer changes. |
| `TASK-BOP-ARABA-ALAVA-001` | implemented locally | Promotes `BOP_ARABA_ALAVA` from inventory-only to metadata-only HTML monitor using the official date-scoped BOTHA page. No PDFs/artifacts, candidates, evidence-grade records, broad backfill, downstream writes, VPS, Hermes, systemd, or timer changes. |
| `TASK-BOP-GIPUZKOA-001` | implemented locally | Promotes `BOP_GIPUZKOA` from inventory-only to metadata-only HTML monitor using the official date-scoped BOG summary page. No PDFs/artifacts, candidates, evidence-grade records, broad backfill, downstream writes, VPS, Hermes, systemd, or timer changes. |
| `TASK-BOP-GIRONA-001` | implemented locally | Promotes `BOP_GIRONA` from inventory-only to metadata-only current-bulletin HTML monitor using the official DDGI BOP page. The monitor emits no records on date mismatch and stores PDF endpoints as official metadata only with `pdf_endpoint_not_downloaded`; no PDFs/artifacts, candidates, evidence-grade records, broad backfill, downstream writes, VPS, Hermes, systemd, or timer changes. |
| `TASK-BOP-HUESCA-001` | implemented locally | Promotes `BOP_HUESCA` from inventory-only to metadata-only current-bulletin HTML monitor using the official BOPH summary page. The monitor emits no records on date mismatch and stores PDF endpoints as official metadata only with `pdf_endpoint_not_downloaded`; no PDFs/artifacts, candidates, evidence-grade records, broad backfill, downstream writes, VPS, Hermes, systemd, or timer changes. |
| `TASK-BOP-CACERES-001` | implemented locally | Promotes `BOP_CACERES` from inventory-only to metadata-only API monitor using the official JSON calendar and announcement services. The monitor stores public announcement URLs as metadata and marks PDF endpoints as not downloaded; no PDFs/artifacts, candidates, evidence-grade records, broad backfill, downstream writes, VPS, Hermes, systemd, or timer changes. |
| `TASK-BOP-GUADALAJARA-001` | implemented locally | Promotes `BOP_GUADALAJARA` from inventory-only to metadata-only RSS monitor using the official Joomla RSS feed. Feed summaries may embed bulletin content and PDF links as metadata only; no PDFs/artifacts, candidates, evidence-grade records, broad backfill, downstream writes, VPS, Hermes, systemd, or timer changes. |
| `TASK-BOP-LAS-PALMAS-001` | implemented locally | Promotes `BOP_LAS_PALMAS` from inventory-only to metadata-only bulletin-level HTML monitor using the official date-scoped summary page. No PDFs/artifacts, candidates, evidence-grade records, broad backfill, downstream writes, VPS, Hermes, systemd, or timer changes. |
| `TASK-BOP-SALAMANCA-001` | blocked | Date-scoped HTML is parseable locally, but project VPS fetch to the official sede timed out at 60 seconds. Kept inventory-only; no monitor promotion, PDFs/artifacts, candidates, evidence-grade records, broad backfill, downstream writes, Hermes, systemd, or timer changes. |
| `TASK-BOP-SANTA-CRUZ-TENERIFE-001` | implemented locally | Promotes `BOP_SANTA_CRUZ_TENERIFE` from inventory-only to metadata-only bulletin-level HTML monitor using the official date-scoped summary page. No PDFs/artifacts, candidates, evidence-grade records, broad backfill, downstream writes, VPS, Hermes, systemd, or timer changes. |
| `TASK-BOP-TARRAGONA-001` | implemented locally | Promotes `BOP_TARRAGONA` from inventory-only to metadata-only HTML monitor using the official date-scoped BOPT announcement page. The monitor reads page one only; no PDFs/artifacts, candidates, evidence-grade records, broad backfill, downstream writes, VPS, Hermes, systemd, or timer changes. |
| `TASK-BOP-TERUEL-001` | implemented locally | Promotes `BOP_TERUEL` from inventory-only to metadata-only current-bulletin HTML monitor using the official XPages BOPT page. The monitor emits no records on date mismatch; no PDFs/artifacts, candidates, evidence-grade records, broad backfill, downstream writes, VPS, Hermes, systemd, or timer changes. |
| `TASK-BOP-CADIZ-001` | implemented locally | Promotes `BOP_CADIZ` from inventory-only to metadata-only HTML monitor using the official current BOP Cadiz landing page. The monitor records bulletin-level metadata and does not download PDFs/artifacts, create candidates, create evidence-grade records, run broad backfills, write downstream, or change Hermes/systemd timers. |
| `TASK-BOP-CIUDAD-REAL-001` | implemented locally | Promotes `BOP_CIUDAD_REAL` from inventory-only to metadata-only HTML monitor using the official date-scoped `bop.dipucr.es` page. PDF endpoints are stored only as official metadata and marked `pdf_endpoint_not_downloaded`; no PDFs/artifacts, candidates, evidence-grade records, broad backfill, downstream writes, Hermes, systemd, or timer changes. |
| `TASK-BOP-CUENCA-001` | blocked | Date-scoped Liferay BOP page parses locally with browser-compatible headers, but the project VPS is rejected by the official `volt-adc` WAF with `403 Forbidden`. Kept inventory-only; no monitor promotion, PDFs/artifacts, candidates, evidence-grade records, broad backfill, downstream writes, Hermes, systemd, or timer changes. |
| `TASK-BOP-OURENSE-001` | implemented locally | Promotes `BOP_OURENSE` from inventory-only to metadata-only API monitor using the official Angular portal backing endpoint `/portalapi/api/boletin/getFecha/{yyyymmdd}`. Edict HTML endpoints are stored only as official metadata; no PDFs/artifacts, candidates, evidence-grade records, broad backfill, downstream writes, Hermes, systemd, or timer changes. |
| `TASK-BOP-ZARAGOZA-001` | blocked | Local parser spike succeeded against the official latest-bulletin BOPZ page, but the project VPS times out connecting to `boletin.dpz.es:443`. Kept inventory-only; no monitor promotion, PDFs/artifacts, candidates, evidence-grade records, broad backfill, downstream writes, Hermes, systemd, or timer changes. |
| `TASK-AUTONOMIC-WAVE-001` | proposed | Implement the highest-confidence autonomous monitors from agent review in order: `BOR` XML API, then `DOCM` HTML summary, then `BON` calendar JSON plus summary HTML. Preview-only first; no PDF downloads, evidence-grade promotion, downstream writes, or live MCP fetches. |
| `TASK-OFFICIAL-SOURCES-BOA-DOCUMENT-URL-MAPPING-001` | completed read-only | Verified BOA document-level official URL mapping after the metadata backfill. `1384/1384` BOA documents have persisted `url_pdf`; `url_html` and `url_xml` remain empty because the BOA date JSON exposes document object links through `UrlPdf`. Confirmed `2026-06-05` comedor sample `BOA:007958917` maps to `MLKOB=1451277650606`. No parser changes, candidates writes, evidence-grade promotion, artifact downloads, downstream writes, runtime, systemd, or timer changes. Report: `docs/reports/boa-document-url-mapping-2026-06-14.md`. |
| `TASK-SOURCE-HTML-MONITOR-HELPERS-001` | deferred | Only extract additional shared helpers after another source proves repeated duplication; do not create a broad generic provincial framework yet. |

## Guardrails

- Do not create source candidates by default.
- Do not create evidence-grade records by default.
- Do not download PDFs without explicit scoped artifact commands.
- Do not run broad backfills from registry work.
- Do not treat RSS/Atom discovery records as evidence or candidates.
- Do not fetch live feeds through MCP source-coverage tools.
- Do not fetch live API data through MCP source-coverage tools.
- Do not write to downstream product repositories.
- Do not run VPS or production DB operations unless a separate task explicitly authorizes them.
- Do not expand candidates or evidence-grade workflows without explicit approval.
- Do not claim all sources are green while any registered source is degraded/manual-review.
- Do not treat registry presence, `monitor_validated`, or `monitor_support=available` as product readiness.
